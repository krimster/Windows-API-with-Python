## Note

Unlike previous examples, `DnsGetCacheDataTable` is **not** a documented Win32 API.

It is an exported function from `dnsapi.dll` that has been discovered through reverse engineering and is commonly used by researchers and security professionals to enumerate the local DNS resolver cache.

Because it is undocumented, Microsoft does not guarantee its behaviour across Windows versions.